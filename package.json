{
    "private": true,
    "scripts": {
        "dev": "npm run development",
        "development": "mix",
        "watch": "mix watch",
        "watch-poll": "mix watch -- --watch-options-poll=1000",
        "hot": "mix watch --hot",
        "prod": "npm run production",
        "production": "mix --production",
        "sass:build": "sass resources/scss:public/assets/css --style=compressed --no-source-map",
        "sass:watch": "sass resources/scss:public/assets/css --style=compressed --no-source-map --watch"
    },
    "dependencies": {
        "bootstrap": "^5.0.1"
    }
}
